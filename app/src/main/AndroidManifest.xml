<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="saini.ayush.playerground">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlayerGround">
        <activity android:name=".media_store.GalleryActivity"/>
        <activity android:name=".animations.NoAnimationActivity" />
        <activity android:name=".animations.LaunchRocketValueAnimatorAnimationActivity" />
        <activity android:name=".animations.ColorAnimationActivity" />
        <activity android:name=".custom_views.HomeActivity" />
        <activity android:name=".animations.AnimActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>